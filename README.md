# Elcoder: Локальный Чат-бот для Кодинга

Elcoder - это интеллектуальный локальный ассистент для программирования на основе DeepSeek-coder, Ollama, LangChain и Gradio. Он предназначен для помощи в написании кода, решении задач программирования и работе с файлами - все это локально на вашем компьютере.

![Elcoder Logo](assets/elcoder-banner.png)

## Основные возможности

- 🤖 Генерация и объяснение кода с высоким качеством
- 📁 Создание и редактирование файлов программ
- 🚀 Выполнение кода Python в реальном времени
- 💬 Сохранение истории диалогов для продолжения работы
- 🏠 Полностью локальное использование без интернета
- 🎨 Гибкий интерфейс с возможностью кастомизации

## Системные требования

- **Операционная система**: Windows 10/11, macOS, или Linux
- **Python**: версия 3.10 или выше
- **Свободное место на диске**: минимум 5 ГБ (для установки моделей)
- **Оперативная память**: рекомендуется минимум 8 ГБ, оптимально 16 ГБ
- **GPU**: опционально, но рекомендуется для более быстрой работы моделей

## Установка

### 1. Установка Python 3.10+

Скачайте и установите Python 3.10 или более новую версию с [официального сайта](https://www.python.org/downloads/).

Убедитесь, что Python добавлен в переменную PATH вашей системы.

### 2. Установка Ollama

Ollama — это платформа для запуска моделей локально.

#### Windows
1. Скачайте установщик с [официального сайта Ollama](https://ollama.ai/download)
2. Запустите установщик и следуйте инструкциям
3. После установки запустите Ollama из меню Пуск

#### macOS
1. Скачайте установщик с [официального сайта Ollama](https://ollama.ai/download)
2. Установите приложение в папку Applications
3. Запустите Ollama

#### Linux
```bash
curl -fsSL https://ollama.ai/install.sh | sh
ollama serve
```

### 3. Клонирование репозитория Elcoder и переход в папку проекта

```bash
# Клонируйте репозиторий
git clone https://github.com/ваш-аккаунт/elcoder.git

# Перейдите в директорию проекта
cd elcoder
```

### 4. Создание и активация виртуального окружения (рекомендуется)

Виртуальное окружение позволяет изолировать зависимости проекта от системного Python, что предотвращает конфликты версий библиотек и упрощает управление проектом.

**Важно**: Все команды ниже нужно выполнять, находясь в папке проекта `elcoder`.

#### Windows

```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
# В командной строке (cmd):
venv\Scripts\activate.bat

# Или в PowerShell:
venv\Scripts\Activate.ps1

# Проверка активации - должно показать путь к Python внутри venv
where python
# или
python --version
```

#### macOS/Linux

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения
source venv/bin/activate

# Проверка активации - должно показать путь к Python внутри venv
which python
# или 
python --version
```

После активации виртуального окружения в начале командной строки появится префикс `(venv)`, например:
```
(venv) C:\Users\username\elcoder>
```
Это указывает на то, что окружение активно и все команды Python будут использовать изолированную среду.

### 5. Установка зависимостей

После активации виртуального окружения установите необходимые зависимости:

```bash
pip install -r requirements.txt
```

Убедитесь, что все зависимости установлены успешно:

```bash
pip list
```

### 6. Загрузка модели DeepSeek-coder

Запустите следующую команду для загрузки модели через Ollama:

```bash
ollama pull deepseek-coder:6.7b-instruct-q4_K_M
```

Этот процесс может занять некоторое время в зависимости от скорости вашего интернет-соединения. Модель занимает около 4-5 ГБ.

### 7. Деактивация виртуального окружения (когда закончите работу)

Когда вы закончите работу с Elcoder, вы можете деактивировать виртуальное окружение:

```bash
deactivate
```

## Быстрый старт

После установки всех компонентов (убедитесь, что виртуальное окружение активировано):

```bash
# Запустить Elcoder
python run.py
```

После запуска, откройте в браузере адрес: `http://localhost:7860`

## Режимы интерфейса

Elcoder предлагает три режима интерфейса:

```bash
# Базовый режим
python run.py --ui basic

# Расширенный режим
python run.py --ui enhanced

# Полный режим (по умолчанию)
python run.py --ui custom
```

## Параметры запуска

```bash
# Запуск на другом порту
python run.py --port 8080

# Запуск с публичной ссылкой
python run.py --share

# Запуск с другой моделью
python run.py --model codellama:7b-instruct-q4_K_M
```

## API интеграция

Elcoder предоставляет API для интеграции с другими инструментами:

```bash
# Запуск API-сервера
python api.py

# Доступен по адресу http://localhost:8000
```

API эндпоинты:
- `POST /chat` - отправка сообщений
- `GET /files` - список файлов
- `GET /files/{filename}` - чтение файла
- `POST /files` - создание файла
- `POST /execute` - выполнение кода
- `GET /config` - текущая конфигурация

## Использование

### Основные функции

1. **Чат**: Задавайте вопросы по программированию, запрашивайте код, просите объяснить концепции
2. **Работа с файлами**: 
   - Создание файлов: перейдите на вкладку "Файлы", введите имя файла, напишите код и нажмите "Сохранить файл"
   - Чтение файлов: введите имя файла и нажмите "Прочитать файл"
   - Список файлов: нажмите "Список файлов" для просмотра доступных файлов
3. **Запуск кода**:
   - Введите код Python на вкладке "Запуск кода"
   - Нажмите "Выполнить код" для запуска и просмотра результатов

### Команды чата

В чате вы можете использовать специальные команды:

- `/file list` - получить список файлов
- `/file read имя_файла` - прочитать содержимое файла
- `/file create имя_файла содержимое` - создать файл с указанным содержимым

## Кастомизация

Подробное руководство по кастомизации Elcoder доступно в файле [CUSTOMIZATION.md](CUSTOMIZATION.md).

## Решение проблем

### Ollama не запускается

1. Убедитесь, что Ollama установлен корректно
2. Перезапустите сервис Ollama:
   ```bash
   # Windows: перезапустите из меню Пуск
   # macOS: перезапустите приложение
   # Linux
   sudo systemctl restart ollama
   ```

### Модель не загружается

1. Проверьте наличие доступных моделей:
   ```bash
   ollama list
   ```
2. Попробуйте загрузить модель вручную:
   ```bash
   ollama pull deepseek-coder:6.7b-instruct-q4_K_M
   ```

### Проблемы с Python или зависимостями

```bash
pip install --upgrade pip
pip install --upgrade -r requirements.txt
```

### Недостаточно памяти

Если у вас возникают проблемы с недостатком памяти, попробуйте:
1. Использовать меньшую квантизованную модель (например, с суффиксом q4_0)
2. Закрыть другие ресурсоемкие приложения
3. Увеличить размер файла подкачки

## Технологии

- **Модель**: DeepSeek-coder через Ollama
- **Backend**: Python 3.10, LangChain, FastAPI
- **Frontend**: Gradio/React
- **Функциональность**: Работа с файлами, выполнение кода, интерактивный чат

## Обновление Elcoder

Для обновления до последней версии:

```bash
cd elcoder
git pull
pip install -r requirements.txt
```

## Лицензия

MIT License

## Автор

[Ваше имя/организация]
